<header class="box-header">
	<div class="container">
    <div class="box-header__inner">
      <a href="/" class="elem-logo">
        <img src="images/required/logo.png" alt="">
      </a>

      <nav class="nav-menu">
        <ul class="nav-menu__list">
          {% for item in menu %}
            <li class="nav-menu__item">
              <a href="{{ item.link }}" class="nav-menu__link">{{ item.name }}</a>
            </li>
          {% endfor %}
        </ul>
      </nav>

      <a href="#" class="elem-btn elem-btn--header">Оставить заявку</a>
    </div>
  </div>
</header>
